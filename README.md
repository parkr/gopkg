Working with go package names & your GOROOT more easily.

1. `gopkg curr` - specifies the name of the package in your current working
   directory
2. `gopkg list` - lists all repos in your GOPATH/src
